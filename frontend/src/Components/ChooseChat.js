import { Typography } from '@mui/material';
import React from 'react';

const ChooseChat = () => {
  return (
    <div>
      <Typography variant='h5'>Choose a chat to begin</Typography>
    </div>
  );
};

export default ChooseChat;
